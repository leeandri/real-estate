{% extends 'base.html.twig' %}

{% block title 'Nexter Agency' %}

{% block body %}
    <div class="jumbotron text-center">
        <h1>Nexter Agency</h1>
        <p>Lorem ipsum dolor sit amet, consectetur adipisicing elit. Adipisci alias commodi, deleniti dolorum
            exercitationem fugiat iste, molestiae mollitia neque obcaecati odit placeat reprehenderit, repudiandae unde
            vel vitae voluptatum? Ipsam, necessitatibus?</p>
    </div>

    <div class="container">
        <h2>Les derniers biens</h2>
        <div class="row flex">

            {% for property in properties %}
                <div class="col-3">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">
                                <a href="{{ path('property.show', {id: property.id, slug: property.slug}) }}">{{ property.title }}</a>
                            </h5>
                            <p class="card-text">{{ property.city }} ({{ property.postalCode }})</p>
                            <div class="text-danger" style="font-weight:300; font-size: 2rem;">{{ property.formattedPrice }} $</div>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
{% endblock %}
